name: alth_lupa
description: Aplikasi automasi pengecekan tandai hadir/presensi SIX ITB.
publish_to: 'none' # Menghindari upload tidak sengaja ke pub.dev

version: 1.0.0+1

# Ini adalah baris yang menyelesaikan error Anda
environment:
  sdk: '^3.10.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Library yang dibutuhkan sesuai dengan main.dart di Canvas
  webview_flutter: ^4.7.0
  shared_preferences: ^2.2.3
  html: ^0.15.4
  flutter_local_notifications: ^17.1.2
  url_launcher: ^6.2.6

  flutter_background_service: ^5.0.0
  flutter_background_service_android: ^6.3.1


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
